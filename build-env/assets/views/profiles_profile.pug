extends _layout.pug

block head
  meta(property="og:site_name" content="RyuNET")
  meta(property="og:title" content=`${profile.name} · player info`)
  meta(property="og:description" content=ogDescription)
  meta(property="og:image" content=ogImage)
  meta(name="theme-color" content="#00d1b2")
  meta(name="twitter:card" content="summary")


  style.
    .profile-header {
      position: relative;
      margin-bottom: 3rem;
      border-radius: 8px;
      overflow: hidden;
      background-color: #2a2a2a;
    }
    .profile-banner {
      height: 280px;
      background-image: url('#{profile.bannerUrl ? "/uploads/" + profile.bannerUrl : "/static/img/default-banner.png"}');
      background-size: cover;
      background-position: center;
      position: relative;
    }
    /* Score Modal Custom Styles */
    #score-detail-modal .modal-content { background: transparent; overflow: visible; }
    .score-modal-content { font-family: 'Outfit', sans-serif, system-ui; background: rgba(13, 13, 20, 0.97); border-radius: 16px; box-shadow: 0 0 0 1px rgba(255,255,255,0.08), 0 20px 60px rgba(0,0,0,0.8); color: #fff; padding: 0; overflow: hidden; width: 820px; max-width: 95vw; animation: modal-in 0.25s cubic-bezier(0.175, 0.885, 0.32, 1.275) both; }
    @keyframes modal-in { from { transform: scale(0.85) translateY(20px); opacity: 0; } to { transform: scale(1) translateY(0); opacity: 1; } }
    .score-header { padding: 20px 24px 16px; position: relative; background: linear-gradient(135deg, #1c1c2e 0%, #12121e 100%); border-bottom: 1px solid rgba(255,255,255,0.06); display: flex; align-items: center; gap: 16px; }
    .score-header-jacket { width: 72px; height: 72px; border-radius: 8px; object-fit: cover; flex-shrink: 0; border: 1px solid rgba(255,255,255,0.12); background: #111; }
    .score-header-text { flex: 1; min-width: 0; }
    .score-header h2 { margin: 0 0 6px 0; font-size: 24px; font-weight: 800; letter-spacing: -0.5px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 100%; color: #fff; }
    .score-subtitle { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; margin-top: 4px; }
    .score-played-by { display: flex; align-items: center; gap: 8px; margin-top: 8px; }
    .score-played-by-avatar { width: 24px; height: 24px; border-radius: 50%; object-fit: cover; border: 1px solid rgba(255,255,255,0.2); flex-shrink: 0; }
    .score-played-by-text { font-size: 12px; color: rgba(255,255,255,0.45); font-weight: 500; }
    .score-played-by-name { font-size: 12px; color: rgba(255,255,255,0.8); font-weight: 700; margin-left: 2px; }
    .diff-badge { padding: 3px 12px; border-radius: 4px; font-weight: 700; font-size: 13px; letter-spacing: 1px; background: rgba(255,255,255,0.12); border: 1px solid rgba(255,255,255,0.2); text-transform: uppercase; }
    .level-badge { padding: 3px 10px; border-radius: 4px; font-weight: 800; font-size: 14px; background: rgba(255,255,255,0.06); color: rgba(255,255,255,0.55); border: 1px solid rgba(255,255,255,0.1); letter-spacing: 0.5px; }
    .score-body { display: flex; padding: 28px 24px 28px 20px; gap: 20px; position: relative; background: linear-gradient(180deg, #0f0f1a 0%, #0a0a12 100%); align-items: center; overflow: visible; }
    .score-rank-container { flex: 0 0 160px; min-width: 160px; display: flex; justify-content: center; align-items: center; position: relative; padding: 14px 10px; overflow: visible; }
    .score-rank-container::before { content: ''; position: absolute; width: 140px; height: 140px; border-radius: 50%; background: var(--rank-color, rgba(255, 120, 0, 0.2)); filter: blur(35px); z-index: 0; }
    #modal-rank { font-size: 90px; font-weight: 900; font-style: italic; line-height: 1.3; padding: 4px 10px; position: relative; z-index: 1; background: var(--rank-gradient, linear-gradient(180deg, #ffb84d 0%, #ff6600 100%)); -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; filter: drop-shadow(0 4px 12px var(--rank-shadow, rgba(255,102,0,0.5))); user-select: none; white-space: nowrap; display: inline-block; }
    #modal-rank[data-grade="S"] { --rank-gradient: linear-gradient(180deg, #ffe066 0%, #ffaa00 100%); --rank-shadow: rgba(255,170,0,0.6); --rank-color: rgba(255,170,0,0.2); font-size: 92px; }
    #modal-rank[data-grade="AAA+"] { --rank-gradient: linear-gradient(180deg, #ff88cc 0%, #cc44aa 100%); --rank-shadow: rgba(200,50,150,0.6); --rank-color: rgba(200,50,150,0.2); font-size: 50px; }
    #modal-rank[data-grade="AAA"] { --rank-gradient: linear-gradient(180deg, #bb88ff 0%, #7733cc 100%); --rank-shadow: rgba(100,50,200,0.6); --rank-color: rgba(100,50,200,0.2); font-size: 64px; }
    #modal-rank[data-grade="AA+"] { --rank-gradient: linear-gradient(180deg, #66aaff 0%, #2255cc 100%); --rank-shadow: rgba(30,80,200,0.6); --rank-color: rgba(30,80,200,0.2); font-size: 58px; }
    #modal-rank[data-grade="AA"] { --rank-gradient: linear-gradient(180deg, #66aaff 0%, #2255cc 100%); --rank-shadow: rgba(30,80,200,0.6); --rank-color: rgba(30,80,200,0.2); font-size: 74px; }
    #modal-rank[data-grade="A+"] { --rank-gradient: linear-gradient(180deg, #66ffdd 0%, #00aa88 100%); --rank-shadow: rgba(0,150,100,0.5); --rank-color: rgba(0,150,100,0.2); font-size: 68px; }
    #modal-rank[data-grade="A"] { --rank-gradient: linear-gradient(180deg, #66ffdd 0%, #00aa88 100%); --rank-shadow: rgba(0,150,100,0.5); --rank-color: rgba(0,150,100,0.2); font-size: 86px; }
    .score-stats { flex: 1; display: flex; flex-direction: column; justify-content: center; gap: 6px; }
    .score-number { font-size: 58px; font-weight: 200; letter-spacing: -2px; line-height: 1; font-variant-numeric: tabular-nums; }
    .score-meta-row { display: flex; gap: 20px; margin-top: 8px; }
    .score-meta-item { display: flex; flex-direction: column; gap: 2px; }
    .score-meta-label { font-size: 10px; letter-spacing: 1.5px; text-transform: uppercase; color: rgba(255,255,255,0.4); font-weight: 600; }
    .score-meta-value { font-size: 22px; font-weight: 700; color: #fff; font-variant-numeric: tabular-nums; }
    #modal-maxchain { color: #44ffcc; }
    #modal-exscore { color: #ffcc44; }
    .score-medal-row { margin-top: 10px; }
    #modal-medal { display: inline-block; padding: 4px 14px; border-radius: 20px; font-size: 12px; font-weight: 700; letter-spacing: 0.8px; text-transform: uppercase; background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); }
    .judgements-container { flex: 0 0 220px; background: rgba(0,0,0,0.4); border-radius: 10px; padding: 18px; border: 1px solid rgba(255,255,255,0.05); display: flex; flex-direction: column; gap: 14px; }
    .judgements-title { font-size: 10px; letter-spacing: 2px; text-transform: uppercase; color: rgba(255,255,255,0.3); font-weight: 600; border-bottom: 1px solid rgba(255,255,255,0.06); padding-bottom: 8px; margin-bottom: 2px; }
    .judgement-row { display: flex; align-items: center; gap: 8px; }
    .j-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
    .j-label { flex: 1; font-size: 12px; font-weight: 700; letter-spacing: 0.5px; text-transform: uppercase; opacity: 0.85; }
    .j-value { font-size: 20px; font-weight: 700; font-variant-numeric: tabular-nums; text-align: right; min-width: 55px; }
    .j-sub-row { display: flex; justify-content: flex-end; gap: 12px; font-size: 11px; color: rgba(255,255,255,0.4); font-weight: 600; margin-top: -8px; padding-right: 2px; }
    .j-sub-item { display: flex; align-items: center; gap: 4px; }
    .j-sub-item .arrow-early { color: #44bbff; }
    .j-sub-item .arrow-late { color: #ff8844; }

    .j-row-crit .j-dot { background: #ffe600; }
    .j-row-crit .j-label, .j-row-crit .j-value { color: #ffe600; }
    .j-row-near .j-dot { background: #44ffcc; }
    .j-row-near .j-label, .j-row-near .j-value { color: #44ffcc; }
    .j-row-error .j-dot { background: #ff3344; }
    .j-row-error .j-label, .j-row-error .j-value { color: #ff3344; }
    .live-recent-play { animation: live-recent-play-in 1.6s ease-out; }
    @keyframes live-recent-play-in {
      0% { background-color: rgba(68,255,204,0.25); }
      100% { background-color: transparent; }
    }
    .score-card, .notification li { transition: transform 0.15s ease, background-color 0.15s ease; cursor: pointer !important; }
    .score-card:hover, .notification li:hover { transform: translateY(-2px); filter: brightness(1.1); }
    .media-overlay {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.5);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      opacity: 0;
      transition: opacity 0.2s;
      cursor: pointer;
      font-weight: bold;
      font-size: 1.1rem;
    }
    .profile-banner:hover .media-overlay, .profile-avatar:hover .media-overlay {
      opacity: 1;
    }
    .hidden-file-input {
      display: none;
    }
    .profile-info-bar {
      background-color: #1a1a1a;
      padding: 1rem 1.5rem;
      display: flex;
      align-items: center;
      min-height: 80px;
    }
    .profile-avatar {
      position: absolute;
      bottom: 20px;
      left: 30px;
      border: 4px solid #1a1a1a;
      border-radius: 16px;
      background-color: #fff;
      overflow: hidden;
      width: 150px;
      height: 150px;
      z-index: 10;
    }
    .profile-avatar img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .profile-name-section {
      margin-left: 190px; /* Avatar width + offset */
      flex-grow: 1;
    }
    .profile-name {
      font-size: 1.5rem;
      font-weight: bold;
      color: #fff;
      margin-bottom: 0.25rem;
    }
    .stats-box {
      background-color: #242424;
      border-radius: 8px;
      padding: 1.5rem;
      height: 100%;
    }
    .stat-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 0.5rem;
      font-size: 0.9rem;
    }
    .stat-label {
      color: #aaa;
    }
    .stat-value {
      font-weight: bold;
      color: #fff;
    }
    .rank-display {
      font-size: 2rem;
      font-weight: bold;
      color: #f1c40f;
    }
    .profile-bio-section {
      background: rgba(0,0,0,0.25);
      border: 1px solid rgba(255,255,255,0.07);
      border-radius: 10px;
      padding: 1.25rem 1.5rem;
      margin-bottom: 1.5rem;
    }
    .profile-bio-label {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: #888;
      margin-bottom: 0.4rem;
    }
    .profile-bio-text {
      color: #ddd;
      font-size: 1rem;
      line-height: 1.75;
      /* Auto-expand: no fixed height, grows with content up to max */
      min-height: 3em;
      max-height: 22em;
      overflow-y: auto;
      overflow-x: hidden;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.15) transparent;
    }
    .profile-bio-text p { margin-bottom: 0.6em; }
    .profile-bio-text p:last-child { margin-bottom: 0; }
    .profile-bio-text h1, .profile-bio-text h2, .profile-bio-text h3 { color: #fff; font-weight: 700; margin: 0.8em 0 0.4em; }
    .profile-bio-text h1 { font-size: 1.4rem; }
    .profile-bio-text h2 { font-size: 1.2rem; }
    .profile-bio-text h3 { font-size: 1.05rem; }
    .profile-bio-text strong { color: #fff; }
    .profile-bio-text em { color: #ccc; }
    .profile-bio-text a { color: #66c2ff; text-decoration: underline; }
    .profile-bio-text ul, .profile-bio-text ol { padding-left: 1.5em; margin-bottom: 0.5em; }
    .profile-bio-text li { margin-bottom: 0.2em; }
    .profile-bio-text code { background: rgba(255,255,255,0.08); border-radius: 4px; padding: 1px 5px; font-family: monospace; font-size: 0.9em; }
    .profile-bio-text blockquote { border-left: 3px solid rgba(255,255,255,0.2); padding-left: 1em; color: #aaa; margin: 0.5em 0; }
    /* Markdown toolbar */
    .bio-toolbar { display: flex; gap: 4px; flex-wrap: wrap; padding: 6px 8px; background: #1a1a1a; border: 1px solid rgba(255,255,255,0.1); border-bottom: none; border-radius: 8px 8px 0 0; }
    .bio-toolbar-btn { background: none; border: 1px solid transparent; color: #aaa; border-radius: 5px; width: 30px; height: 28px; display: inline-flex; align-items: center; justify-content: center; font-size: 0.85rem; cursor: pointer; transition: all 0.15s; font-family: serif; font-weight: bold; }
    .bio-toolbar-btn:hover { background: rgba(255,255,255,0.1); border-color: rgba(255,255,255,0.2); color: #fff; }
    .bio-toolbar-sep { width: 1px; background: rgba(255,255,255,0.1); margin: 3px 2px; }
    .bio-textarea-wrap textarea { border-radius: 0 0 8px 8px !important; border-top: none !important; resize: none; min-height: 120px; max-height: 400px; overflow-y: auto; }
    .stats-horizontal { display: flex; justify-content: space-around; background: #222; border-radius: 12px; padding: 20px 10px; margin-bottom: 2rem; border: 1px solid rgba(255,255,255,0.05); box-shadow: 0 4px 6px rgba(0,0,0,0.2); }
    .stat-item { display: flex; flex-direction: column; align-items: center; }
    .stat-label { font-size: 0.75rem; color: #888; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 6px; }
    .stat-val { font-size: 1.75rem; font-weight: bold; color: #fff; }
    .stat-val.gold { color: #ffcc00; }
    .bio-edit-btn {
      background: rgba(255,255,255,0.1);
      border: none;
      color: #eee;
      border-radius: 16px;
      transition: background 0.2s;
    }
    .bio-edit-btn:hover {
      background: rgba(255,255,255,0.25);
      color: #fff;
    }
    .score-list { list-style: none; padding: 0; margin: 0; }
    .score-card { display: flex; align-items: center; background: #2c2c2c; border-radius: 12px; padding: 10px 16px; margin-bottom: 8px; transition: background 0.2s; border: 1px solid rgba(255,255,255,0.05); }
    .score-card:hover { background: #363636; border-color: rgba(255,255,255,0.1); }
    .score-jacket { width: 56px; height: 56px; border-radius: 8px; margin-right: 16px; object-fit: cover; background: #111; border: 1px solid rgba(255,255,255,0.1); }
    .score-info { flex: 1; display: flex; flex-direction: column; justify-content: center; overflow: hidden; text-align: left; }
    .score-title { font-weight: 600; color: #fff; font-size: 1.05rem; margin-bottom: 4px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .score-meta { display: flex; align-items: center; font-size: 0.8rem; color: #888; }
    .diff-badge { padding: 3px 8px; border-radius: 6px; font-weight: bold; font-size: 0.75rem; margin-right: 10px; background: #444; color: #fff; text-transform: uppercase; letter-spacing: 0.5px; }
    .score-stats { display: flex; align-items: center; gap: 24px; text-align: right; margin-left: auto; }
    .stat-col { display: flex; flex-direction: column; align-items: flex-end; min-width: 90px; }
    .score-main-stat { font-size: 1.25rem; font-weight: bold; color: #fff; }
    .score-main-stat.is-gold { color: #ffd700; }
    .score-sub-stat { font-size: 0.75rem; color: #888; text-transform: uppercase; letter-spacing: 0.5px; }
    .score-date { font-size: 0.8rem; color: #aaa; margin-left: 24px; white-space: nowrap; width: 85px; text-align: right; }
    .show-more-container { text-align: center; margin-top: 15px; margin-bottom: 30px; }
    .show-more-btn { background: #333; border: 1px solid #444; color: #eee; padding: 8px 24px; border-radius: 20px; font-size: 0.85rem; font-weight: bold; cursor: pointer; transition: all 0.2s ease; letter-spacing: 1px; }
    .show-more-btn:hover { background: #444; border-color: #555; color: #fff; transform: translateY(-1px); }
    .section-title { text-align: center; margin-bottom: 1.5rem !important; position: relative; font-weight: bold; letter-spacing: 1px; text-transform: uppercase; font-size: 1.1rem; color: #fff; border-bottom: 2px solid #333; padding-bottom: 10px; }
    
    .clear-puc { border-left: 4px solid #ffd700 !important; }
    .clear-uc { border-left: 4px solid #ff4081 !important; }
    .clear-exc { border-left: 4px solid #00d1b2 !important; }
    .clear-comp { border-left: 4px solid #209cee !important; }
    
    .clear-fc { border-left: 4px solid #ffd700 !important; }
    .clear-exh { border-left: 4px solid #ff4081 !important; }
    .clear-hard { border-left: 4px solid #f00 !important; }
    .clear-clear { border-left: 4px solid #209cee !important; }
    .clear-easy { border-left: 4px solid #00d1b2 !important; }
    .clear-assist { border-left: 4px solid #b86bff !important; }
    .clear-fail { border-left: 4px solid #555 !important; }
    
    .tier-lustrous { background: linear-gradient(90deg, #ff8a00, #e52e71, #ff00ff); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-weight: 900 !important; }
    .tier-radiant { color: #55ffff !important; font-weight: 800 !important; text-shadow: 0 0 8px rgba(85,255,255,0.4); }
    .tier-rhodium { color: #ff99cc !important; font-weight: 800 !important; }
    .tier-platinum { color: #00e5ff !important; font-weight: 800 !important; }
    .tier-gold { color: #ffd700 !important; font-weight: 700 !important; }
    .tier-silver { color: #c0c0c0 !important; font-weight: 600 !important; }
    .tier-bronze { color: #cd7f32 !important; font-weight: 600 !important; }
    .tier-iron { color: #a19d94 !important; font-weight: 600 !important; }
    
    .stats-horizontal { display: flex; justify-content: space-around; align-items: center; background: #242424; border-radius: 12px; padding: 25px 20px; margin-bottom: 2rem; box-shadow: 0 4px 15px rgba(0,0,0,0.2); }
    .stat-item { text-align: center; flex: 1; }
    .stat-label { font-size: 0.8rem; color: #888; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }
    .stat-val { font-size: 2rem; font-weight: bold; color: #fff; }
    .stat-val.gold { color: #ffd700; }
    .pane-container { max-width: 900px; margin: 0 auto; }

    .profile-details-bar { display: flex; flex-wrap: wrap; align-items: center; gap: 6px 18px; margin-top: 0.5rem; font-size: 0.82rem; color: #aaa; }
    .profile-detail-item { display: inline-flex; align-items: center; gap: 5px; white-space: nowrap; }
    .profile-detail-item a { color: #aaa; text-decoration: none; transition: color 0.2s; }
    .profile-detail-item a:hover { color: #fff; }
    .profile-detail-item .mdi { font-size: 1rem; }
    .edit-profile-btn { background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.15); color: #ccc; border-radius: 20px; padding: 4px 14px; font-size: 0.8rem; font-weight: 600; cursor: pointer; transition: all 0.2s; letter-spacing: 0.5px; display: inline-flex; align-items: center; gap: 6px; }
    .edit-profile-btn:hover { background: rgba(255,255,255,0.18); border-color: rgba(255,255,255,0.3); color: #fff; }
    .ep-modal-backdrop { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.75); z-index: 1000; align-items: center; justify-content: center; }
    .ep-modal-backdrop.is-open { display: flex; }
    .ep-modal { background: #1e1e1e; border: 1px solid rgba(255,255,255,0.1); border-radius: 16px; padding: 2rem; width: 100%; max-width: 520px; max-height: 90vh; overflow-y: auto; position: relative; box-shadow: 0 20px 60px rgba(0,0,0,0.6); animation: ep-in 0.2s ease; }
    @keyframes ep-in { from { opacity: 0; transform: translateY(-16px); } to { opacity: 1; transform: translateY(0); } }
    .ep-modal h2 { font-size: 1.15rem; font-weight: 700; color: #fff; margin-bottom: 1.5rem; display: flex; align-items: center; gap: 8px; }
    .ep-field { margin-bottom: 1rem; }
    .ep-label { display: block; font-size: 0.72rem; text-transform: uppercase; letter-spacing: 1px; color: #888; margin-bottom: 5px; font-weight: 600; }
    .ep-input { width: 100%; background: #2a2a2a; border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; color: #fff; padding: 8px 12px; font-size: 0.9rem; transition: border-color 0.2s; outline: none; }
    .ep-input:focus { border-color: rgba(255,255,255,0.35); }
    .ep-prefix { display: flex; align-items: center; background: #2a2a2a; border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; overflow: hidden; }
    .ep-prefix-label { padding: 8px 10px; color: #666; font-size: 0.85rem; white-space: nowrap; border-right: 1px solid rgba(255,255,255,0.08); }
    .ep-prefix .ep-input { border: none; border-radius: 0; background: transparent; }
    .ep-divider { border: none; border-top: 1px solid rgba(255,255,255,0.07); margin: 1.2rem 0; }
    .ep-section-label { font-size: 0.7rem; text-transform: uppercase; letter-spacing: 1.5px; color: #666; margin-bottom: 0.75rem; font-weight: 700; }
    .ep-close { position: absolute; top: 1rem; right: 1rem; background: none; border: none; color: #888; font-size: 1.4rem; cursor: pointer; line-height: 1; padding: 4px; border-radius: 50%; transition: color 0.2s; }
    .ep-close:hover { color: #fff; }
    .ep-actions { display: flex; gap: 10px; justify-content: flex-end; margin-top: 1.5rem; }

block content
  - const getRankTier = (rank, total) => {
  -   if (!rank || !total) return '';
  -   if (rank <= 3) return 'tier-lustrous';
  -   if (rank <= 10) return 'tier-radiant';
  -   if (rank <= 25) return 'tier-rhodium';
  -   if (rank <= 50) return 'tier-platinum';
  -   const pct = rank / total;
  -   if (pct <= 0.20) return 'tier-gold';
  -   if (pct <= 0.40) return 'tier-silver';
  -   if (pct <= 0.70) return 'tier-bronze';
  -   return 'tier-iron';
  - }
  - let userTierClass = '';
  - if (sdvxRank && sdvxRank.globalRank) {
  -   userTierClass = getRankTier(sdvxRank.globalRank, sdvxRank.totalGlobal);
  - } else if (iidxRank && iidxRank.globalRank) {
  -   userTierClass = getRankTier(iidxRank.globalRank, iidxRank.totalGlobal);
  - }
  - const getSdvxClearClass = (clearVal) => {
  -   const c = Number(clearVal);
  -   if (c === 5) return 'clear-puc';
  -   if (c === 4) return 'clear-uc';
  -   if (c === 3) return 'clear-exc';
  -   if (c === 2) return 'clear-comp';
  -   if (c === 1) return 'clear-fail';
  -   return '';
  - }
  - const getIidxClearClass = (clearVal) => {
  -   const c = Number(clearVal);
  -   if (c === 7) return 'clear-fc';
  -   if (c === 6) return 'clear-exh';
  -   if (c === 5) return 'clear-hard';
  -   if (c === 4) return 'clear-clear';
  -   if (c === 3) return 'clear-easy';
  -   if (c === 2) return 'clear-assist';
  -   if (c === 1) return 'clear-fail';
  -   return '';
  - }
  section.section.is-main-section(style="max-width: 1200px; margin: 0 auto;")
    .profile-header
      .profile-banner
        if isAdmin || isOwner
          form(action=`/profile/${profile.__refid}/media` method="post" enctype="multipart/form-data")
            label.media-overlay
              input.hidden-file-input(type="file" name="banner" accept="image/*" onchange="this.form.submit()")
              span.icon.is-medium: i.mdi.mdi-camera
              span &nbsp; Change Banner
              span(style="font-size: 0.7rem; opacity: 0.7; margin-left: 6px;") (2000×500)
      .profile-avatar
        if profile.avatarUrl
          img(src=`/uploads/${profile.avatarUrl}`)
        else
          img(src="/static/img/avatar.jpg" style="width: 150px; height: 150px; display: block; border-radius: 12px; object-fit: cover;")
        if isAdmin || isOwner
          form(action=`/profile/${profile.__refid}/media` method="post" enctype="multipart/form-data" style="border-radius: 12px; overflow: hidden;")
            label.media-overlay(style="border-radius: 12px;")
              input.hidden-file-input(type="file" name="avatar" accept="image/*" onchange="this.form.submit()")
              span.icon: i.mdi.mdi-camera
      .profile-info-bar
        .profile-name-section
          .profile-name.is-flex.is-align-items-center
            if countryCode && countryCode !== 'xx'
              span.fi(class="fi-" + countryCode data-tooltip=countryCode.toUpperCase() style="margin-right: 0.5rem; border-radius: 2px; border-bottom: none !important;")
            else
              span(style="margin-right: 0.5rem; font-size: 1.2rem;" data-tooltip="Unknown") 🌎
            span(class=userTierClass) #{profile.name}
            
            if myRefid && myRefid !== profile.__refid
              form(action=`/profile/${profile.__refid}/follow` method="POST" style="display: inline-flex; align-items: center; margin-left: 20px;")
                button.button.is-small.is-rounded(type="submit" class=isFollowing ? "is-success" : "is-dark" style="font-weight: bold;")
                  span.icon.is-small: i.mdi(class=isFollowing ? "mdi-account-minus" : "mdi-account-plus")
                  span #{isFollowing ? 'Unfollow' : 'Follow'}
            
            span.tag.is-rounded.is-dark(style="margin-left: 10px; font-weight: bold; font-size: 1rem; padding: 16px 15px; background: #222 !important;")
              span.icon.is-small(style="margin-right: 6px; color: #fff;"): i.mdi.mdi-account
              | #{followerCount.toLocaleString()}
          .profile-meta.is-flex.is-align-items-center(style="margin-top: 0.3rem; font-size: 0.85rem; color: #aaa; gap: 0.5rem;")
            if accountCreatedAt
              - const joinDate = new Date(accountCreatedAt).toLocaleDateString('en-US', { month: 'long', year: 'numeric' })
              .is-flex.is-align-items-center
                span.icon.is-small(style="margin-right: 0.2rem;") #[i.mdi.mdi-calendar-outline]
                span Joined #{joinDate}
              if accountLastLogin
                span(style="color: #555;") &middot;
                - const secsAgo = Math.floor((Date.now() - accountLastLogin) / 1000)
                - const minsAgo = Math.floor(secsAgo / 60)
                - const hoursAgo = Math.floor(minsAgo / 60)
                - const daysAgo = Math.floor(hoursAgo / 24)
                - let lastSeenStr = ''
                - if (secsAgo < 60) { lastSeenStr = 'Currently online' }
                - else if (minsAgo < 60) { lastSeenStr = 'Last seen ' + minsAgo + (minsAgo === 1 ? ' minute' : ' minutes') + ' ago' }
                - else if (hoursAgo < 24) { lastSeenStr = 'Last seen ' + hoursAgo + (hoursAgo === 1 ? ' hour' : ' hours') + ' ago' }
                - else { lastSeenStr = 'Last seen ' + daysAgo + (daysAgo === 1 ? ' day' : ' days') + ' ago' }
                .is-flex.is-align-items-center
                  span.icon.is-small(style="margin-right: 0.2rem;") #[i.mdi.mdi-clock-outline]
                  span #{lastSeenStr}
          
          //- Social / detail bar (osu!-style)
          - const hasDetails = profile.location || profile.interests || profile.occupation || profile.twitter || profile.discord || profile.website
          if hasDetails
            .profile-details-bar
              if profile.location
                span.profile-detail-item
                  i.mdi.mdi-map-marker-outline
                  span #{profile.location}
              if profile.interests
                span.profile-detail-item
                  i.mdi.mdi-heart-outline
                  span #{profile.interests}
              if profile.occupation
                span.profile-detail-item
                  i.mdi.mdi-briefcase-outline
                  span #{profile.occupation}
              if profile.twitter
                span.profile-detail-item
                  i.mdi.mdi-twitter
                  a(href=`https://twitter.com/${profile.twitter}` target="_blank" rel="noopener") @#{profile.twitter}
              if profile.discord
                span.profile-detail-item
                  i.mdi.mdi-discord
                  span #{profile.discord}
              if profile.website
                span.profile-detail-item
                  i.mdi.mdi-link-variant
                  a(href=(profile.website.startsWith('http') ? profile.website : 'https://' + profile.website) target="_blank" rel="noopener") #{profile.website}

          //- Edit Profile button for owner/admin
          if isAdmin || isOwner
            .mt-2
              button.edit-profile-btn(onclick="openEditProfile()")
                i.mdi.mdi-pencil-outline
                | Edit Profile

        if isAdmin
          .is-flex.is-align-items-center(style="gap: 0.5rem; margin-left: auto; flex-shrink: 0; align-self: flex-start; padding-top: 4px;")
            a.button.is-small.is-warning(href=`/admin/profile/${profile.__refid}` title="Edit profile data & cards")
              span.icon.is-small: i.mdi.mdi-account-edit
              span Admin Edit
            if accountUsername
              a.button.is-small.is-info(href=`/admin/account/${accountUsername}` title="Edit account credentials")
                span.icon.is-small: i.mdi.mdi-account-cog
                span Edit Account


    //- Edit Profile Modal
    if isAdmin || isOwner
      .ep-modal-backdrop#ep-modal-backdrop(onclick="closeEditProfileBackdrop(event)")
        .ep-modal
          button.ep-close(onclick="closeEditProfile()" type="button") &times;
          h2
            i.mdi.mdi-account-edit-outline
            | Edit Profile
          form(action=`/profile/${profile.__refid}` method="post")
            

            p.ep-section-label Privacy
            .ep-field
              label.checkbox(style="color: #ccc;")
                input(type="checkbox" name="isPrivate" checked=profile.isPrivate style="margin-right: 8px;")
                | Make my profile private (Hide from leaderboards & guests)

            hr.ep-divider
            p.ep-section-label Personal
            .ep-field
              label.ep-label(for="ep-location") Current location
              input#ep-location.ep-input(type="text" name="location" maxlength="100" placeholder="e.g. My Home" value=(profile.location || ''))
            .ep-field
              label.ep-label(for="ep-interests") Interests
              input#ep-interests.ep-input(type="text" name="interests" maxlength="100" placeholder="e.g. Sleep" value=(profile.interests || ''))
            .ep-field
              label.ep-label(for="ep-occupation") Occupation
              input#ep-occupation.ep-input(type="text" name="occupation" maxlength="100" placeholder="e.g. Student & working" value=(profile.occupation || ''))
            
            hr.ep-divider
            p.ep-section-label Socials
            .ep-field
              label.ep-label(for="ep-twitter") Twitter / X
              .ep-prefix
                span.ep-prefix-label @
                input#ep-twitter.ep-input(type="text" name="twitter" maxlength="50" placeholder="handle" value=(profile.twitter || ''))
            .ep-field
              label.ep-label(for="ep-discord") Discord
              input#ep-discord.ep-input(type="text" name="discord" maxlength="50" placeholder="username" value=(profile.discord || ''))
            .ep-field
              label.ep-label(for="ep-website") Website
              input#ep-website.ep-input(type="url" name="website" maxlength="200" placeholder="https://..." value=(profile.website || ''))
            
            .ep-actions
              button.button.is-dark.is-small(type="button" onclick="closeEditProfile()") Cancel
              button.button.is-primary.is-small(type="submit")
                span.icon.is-small: i.mdi.mdi-content-save-outline
                span Save changes

                
    .profile-bio-section
      .profile-bio-label.is-flex.is-justify-content-space-between.is-align-items-center(style="margin-bottom: 12px;")
        .is-flex.is-align-items-center
          span.icon.is-small.mr-2: i.mdi.mdi-text-account
          span(style="font-weight: 600; letter-spacing: 1px; text-transform: uppercase; font-size: 0.9rem; color: #bbb;") About
        if isAdmin || isOwner
          button.button.is-small.bio-edit-btn(onclick="toggleBioEdit()" id="bio-edit-toggle-btn")
            span.icon.is-small: i.mdi.mdi-pencil
            span Edit
      
      .profile-bio-text.content#bio-display !{bioHtml || '<em style="color:#666;">No bio provided yet.</em>'}
      
      if isAdmin || isOwner
        form#bio-edit-form(action=`/profile/${profile.__refid}` method="post" style="display: none;")
          //- Markdown toolbar
          .bio-toolbar#bio-toolbar
            button.bio-toolbar-btn(type="button" title="Bold" onclick="bioInsert('**','**')")
              b B
            button.bio-toolbar-btn(type="button" title="Italic" onclick="bioInsert('*','*')")
              i I
            button.bio-toolbar-btn(type="button" title="Strikethrough" onclick="bioInsert('~~','~~')" style="font-size:0.8rem; text-decoration: line-through;") S
            .bio-toolbar-sep
            button.bio-toolbar-btn(type="button" title="Heading" onclick="bioInsertLine('## ')" style="font-size:0.8rem; font-family: sans-serif; font-weight:700;") H
            button.bio-toolbar-btn(type="button" title="Quote" onclick="bioInsertLine('> ')" style="font-family: sans-serif;") "
            .bio-toolbar-sep
            button.bio-toolbar-btn(type="button" title="Ordered list" onclick="bioInsertLine('1. ')" style="font-family: sans-serif; font-size: 0.75rem;")
              i.mdi.mdi-format-list-numbered
            button.bio-toolbar-btn(type="button" title="Unordered list" onclick="bioInsertLine('- ')" style="font-family: sans-serif; font-size: 0.75rem;")
              i.mdi.mdi-format-list-bulleted
            .bio-toolbar-sep
            button.bio-toolbar-btn(type="button" title="Link" onclick="bioInsertLink()" style="font-family: sans-serif; font-size: 0.75rem;")
              i.mdi.mdi-link-variant
            button.bio-toolbar-btn(type="button" title="Inline code" onclick="bioInsert('\`','\`')" style="font-family: monospace; font-size: 0.8rem;") &lt;/&gt;
          .bio-textarea-wrap
            textarea.textarea#bio-textarea(name="bio" maxlength="500" placeholder="Write something about yourself... (Markdown supported)") #{profile.bio || ''}
          .field.is-grouped.mt-2
            .control
              button.button.is-primary.is-small(type="submit")
                span.icon.is-small: i.mdi.mdi-content-save-outline
                span Save
            .control
              a.button.is-dark.is-small(onclick="toggleBioEdit()") Cancel

    .tabs.is-centered.is-boxed
      ul
        li.is-active#tab-sdvx
          a(onclick="switchTab('sdvx')")
            span.icon.is-small
              i.mdi.mdi-gamepad-variant
            span SOUND VOLTEX
        li#tab-iidx
          a(onclick="switchTab('iidx')")
            span.icon.is-small
              i.mdi.mdi-record-circle-outline
            span beatmania IIDX

    .pane-container
      #pane-sdvx.mt-5
        if sdvxStats && sdvxStats.totalScores > 0
          .stats-horizontal(style="position: relative; overflow: hidden;")
            if sdvxStats.trend && sdvxStats.trend.length > 0
              - const sMin = Math.min(...sdvxStats.trend)
              - const sMax = Math.max(...sdvxStats.trend)
              - const sDiff = (sMax - sMin) || 1
              - const sPts = sdvxStats.trend.map((v, i) => `${(i / (sdvxStats.trend.length - 1)) * 100},${100 - ((v - sMin) / sDiff) * 100}`).join(' ')
              svg(viewBox="0 0 100 100" preserveAspectRatio="none" style="position: absolute; bottom: 0; left: 0; width: 100%; height: 25px; z-index: 0; pointer-events: none;")
                defs
                  linearGradient#sGrad(x1="0" y1="0" x2="0" y2="1")
                    stop(offset="0%" stop-color="#00e5ff" stop-opacity="0.25")
                    stop(offset="100%" stop-color="#00e5ff" stop-opacity="0")
                polygon(points=`0,100 ${sPts} 100,100` fill="url(#sGrad)")
                polyline(points=sPts fill="none" stroke="#00e5ff" stroke-width="2" vector-effect="non-scaling-stroke")
            
            if sdvxRank && sdvxRank.globalRank
              .stat-item(style="position: relative; z-index: 1;")
                .stat-label Global Rank
                .stat-val(class=getRankTier(sdvxRank.globalRank, sdvxRank.totalGlobal)) ##{sdvxRank.globalRank}
            if sdvxRank && sdvxRank.nationalRank
              .stat-item(style="position: relative; z-index: 1;")
                .stat-label National Rank
                .stat-val(class=getRankTier(sdvxRank.nationalRank, sdvxRank.totalNational)) ##{sdvxRank.nationalRank}
            .stat-item(style="position: relative; z-index: 1;")
              .stat-label Total Scores
              .stat-val #{sdvxStats.totalScores}
            .stat-item(style="position: relative; z-index: 1;")
              .stat-label Volforce
              .stat-val #{Number(sdvxStats.volforce / 1000).toFixed(3)}
              
          if sdvxStats.recentPlays && sdvxStats.recentPlays.length > 0
            h3.section-title Recent Plays
            .notification.is-dark.has-text-left.mb-6
               ul#sdvx-recent-plays-list(style="list-style: none; padding: 0;")
                 each play in sdvxStats.recentPlays
                   li.mb-3(class=(play.clear ? getSdvxClearClass(play.clear) : '') style="padding-left: 10px; border-radius: 4px; cursor: pointer;" onclick="openSdvxScoreModal(this)" data-play=JSON.stringify(play))
                     .is-flex.is-align-items-center
                       img(src=play.jacketUrl onerror="jacketFallback(this)" style="width: 40px; height: 40px; border-radius: 4px; margin-right: 12px; object-fit: cover;")
                       div(style="flex: 1;")
                         div
                           span.icon.is-small(style=`color: ${play.isFirstPlace ? '#ffd700' : '#00d1b2'};`)
                             i.mdi(class=play.isFirstPlace ? 'mdi-crown' : 'mdi-history')
                           span.has-text-weight-bold  #{profile.name}
                           |  played 
                           span.has-text-weight-bold(style="color: #66c2ff;") #{play.title} [#{play.diff}] 
                           |  - #{play.score}
                         if play.dateStr !== 'Unknown'
                           div(style="color: #888; font-size: 0.8rem;") #{play.dateStr}

          if sdvxStats.firstPlaces && sdvxStats.firstPlaces.length > 0
            h3.section-title First Place Scores (#{sdvxStats.firstPlaces.length})
            ul.score-list#sdvx-first-places
              each play, index in sdvxStats.firstPlaces
                li.score-card(style=(index >= 5 ? "display: none;" : "") class=(play.clear ? getSdvxClearClass(play.clear) : '') onclick="openSdvxScoreModal(this)" data-play=JSON.stringify(play))
                  img.score-jacket(src=play.jacketUrl onerror="jacketFallback(this)")
                  .score-info
                    .score-title #{play.title}
                    .score-meta
                      span.diff-badge #{play.diff}
                  .score-stats
                    .stat-col
                      .score-main-stat #{play.score}
                      .score-sub-stat Score
                    .score-date
                      if play.dateStr
                        | #{play.dateStr}
                      else
                        | —
            if sdvxStats.firstPlaces.length > 5
              .show-more-container
                button.show-more-btn(onclick="loadMoreScores('sdvx-first-places', this)") SHOW MORE ⏷

          if sdvxStats.topPlays && sdvxStats.topPlays.length > 0
            h3.section-title Top Plays B50
            ul.score-list#sdvx-top-plays
              each play, index in sdvxStats.topPlays
                li.score-card(style=(index >= 5 ? "display: none;" : "") class=(play.clear ? getSdvxClearClass(play.clear) : '') onclick="openSdvxScoreModal(this)" data-play=JSON.stringify(play))
                  img.score-jacket(src=play.jacketUrl onerror="jacketFallback(this)")
                  .score-info
                    .score-title #{play.title}
                    .score-meta
                      span.diff-badge #{play.diff}
                  .score-stats
                    .stat-col
                      .score-main-stat #{play.score}
                      .score-sub-stat Score
                    .stat-col
                      .score-main-stat.is-gold #{play.volforce}
                      .score-sub-stat Volforce
                    if play.dateStr !== 'Unknown'
                      .score-date #{play.dateStr}
                    else
                      .score-date
            if sdvxStats.topPlays.length > 5
              .show-more-container
                button.show-more-btn(onclick="loadMoreScores('sdvx-top-plays', this)") SHOW MORE ⏷
        else
          .notification.is-dark.has-text-centered.mt-4
            span.icon.is-large: i.mdi.mdi-gamepad-variant-outline.mdi-36px
            p No SDVX data available yet.

      #pane-iidx.mt-5(style="display: none;")
        if iidxStats && iidxStats.totalScores > 0
          .stats-horizontal(style="position: relative; overflow: hidden;")
            if iidxStats.trend && iidxStats.trend.length > 0
              - const iMin = Math.min(...iidxStats.trend)
              - const iMax = Math.max(...iidxStats.trend)
              - const iDiff = (iMax - iMin) || 1
              - const iPts = iidxStats.trend.map((v, i) => `${(i / (iidxStats.trend.length - 1)) * 100},${100 - ((v - iMin) / iDiff) * 100}`).join(' ')
              svg(viewBox="0 0 100 100" preserveAspectRatio="none" style="position: absolute; bottom: 0; left: 0; width: 100%; height: 25px; z-index: 0; pointer-events: none;")
                defs
                  linearGradient#iGrad(x1="0" y1="0" x2="0" y2="1")
                    stop(offset="0%" stop-color="#00e5ff" stop-opacity="0.25")
                    stop(offset="100%" stop-color="#00e5ff" stop-opacity="0")
                polygon(points=`0,100 ${iPts} 100,100` fill="url(#iGrad)")
                polyline(points=iPts fill="none" stroke="#00e5ff" stroke-width="2" vector-effect="non-scaling-stroke")
                
            if iidxRank && iidxRank.globalRank
              .stat-item(style="position: relative; z-index: 1;")
                .stat-label Global Rank
                .stat-val(class=getRankTier(iidxRank.globalRank, iidxRank.totalGlobal)) ##{iidxRank.globalRank}
            if iidxRank && iidxRank.nationalRank
              .stat-item(style="position: relative; z-index: 1;")
                .stat-label National Rank
                .stat-val(class=getRankTier(iidxRank.nationalRank, iidxRank.totalNational)) ##{iidxRank.nationalRank}
            .stat-item(style="position: relative; z-index: 1;")
              .stat-label Total Scores
              .stat-val #{iidxStats.totalScores}
            .stat-item(style="position: relative; z-index: 1;")
              .stat-label SP Dan
              .stat-val #{iidxStats.spDan}
            .stat-item(style="position: relative; z-index: 1;")
              .stat-label DP Dan
              .stat-val #{iidxStats.dpDan}
              
          if iidxStats.recentPlays && iidxStats.recentPlays.length > 0
            h3.section-title 🕒 Recent Plays
            .notification.is-dark.has-text-left.mb-6
               ul#iidx-recent-plays-list(style="list-style: none; padding: 0;")
                 each play in iidxStats.recentPlays
                   li.mb-2(class=(play.clear ? getIidxClearClass(play.clear) : '') style="padding-left: 10px; border-radius: 4px;")
                     span.icon.is-small(style="color: #00d1b2;"): i.mdi.mdi-history
                     span.has-text-weight-bold  #{profile.name}
                     |  played 
                     span.has-text-weight-bold(style="color: #66c2ff;") #{play.title} [#{play.diff}] 
                     |  - #{play.score}
                     if play.dateStr !== 'Unknown'
                       span.is-pulled-right(style="color: #888; font-size: 0.8rem;") #{play.dateStr}

          if iidxStats.firstPlaces && iidxStats.firstPlaces.length > 0
            h3.section-title 🏆 First Place Scores (#{iidxStats.firstPlaces.length})
            ul.score-list#iidx-first-places
              each play, index in iidxStats.firstPlaces
                li.score-card(style=(index >= 5 ? "display: none;" : "") class=(play.clear ? getIidxClearClass(play.clear) : ''))
                  .score-info
                    .score-title #{play.title}
                    .score-meta
                      span.diff-badge #{play.diff}
                  .score-stats
                    .stat-col
                      .score-main-stat #{play.score}
                      .score-sub-stat EX Score
                    .score-date
                      if play.dateStr
                        | #{play.dateStr}
                      else
                        | —
            if iidxStats.firstPlaces.length > 5
              .show-more-container
                button.show-more-btn(onclick="loadMoreScores('iidx-first-places', this)") SHOW MORE ⏷

          if iidxStats.topPlays && iidxStats.topPlays.length > 0
            h3.section-title ⭐ Top Plays
            ul.score-list#iidx-top-plays
              each play, index in iidxStats.topPlays
                li.score-card(style=(index >= 5 ? "display: none;" : "") class=(play.clear ? getIidxClearClass(play.clear) : ''))
                  .score-info
                    .score-title #{play.title}
                    .score-meta
                      span.diff-badge #{play.diff}
                  .score-stats
                    .stat-col
                      .score-main-stat.is-gold #{play.score}
                      .score-sub-stat EX Score
                    if play.dateStr !== 'Unknown'
                      .score-date #{play.dateStr}
                    else
                      .score-date
            if iidxStats.topPlays.length > 5
              .show-more-container
                button.show-more-btn(onclick="loadMoreScores('iidx-top-plays', this)") SHOW MORE ⏷
        else
          .notification.is-dark.has-text-centered.mt-4
            span.icon.is-large: i.mdi.mdi-record-circle-outline.mdi-36px
            p No IIDX data available yet.
          


block scripts
  script.
    const JACKET_DUMMY = 'https://jackets.ryu7w7.xyz/sdvx/jk_dummy.png';
    const MODAL_PLAYER_NAME = '!{profile.name}';
    const MODAL_PLAYER_AVATAR = '!{profile.avatarUrl ? "/uploads/" + profile.avatarUrl : "/static/img/avatar.jpg"}';
    const PROFILE_REFID = '!{profile.__refid}';
    // jacketFallback MUST be defined before any <img> onerror fires.
    function jacketFallback(img) {
      // Try falling back: _4 -> _3 -> _2 -> _1 -> dummy
      // URL pattern: .../folderName/jk_XXXX_N.png
      const src = img.src;
      const m = src.match(/^(.*\/jk_(\d+)_)(\d+)\.png(.*)?$/);
      if (m) {
        const prefix = m[1];
        const current = parseInt(m[3], 10);
        if (current > 1) {
          img.onerror = function() { jacketFallback(img); };
          img.src = prefix + (current - 1) + '.png';
          return;
        }
      }
      img.onerror = null;
      img.src = JACKET_DUMMY;
    }
  script(type="text/javascript" src="/static/js/profile.js")
  script.
    function openEditProfile() {
      document.getElementById('ep-modal-backdrop').classList.add('is-open');
      document.documentElement.classList.add('is-clipped');
    }
    function closeEditProfile() {
      document.getElementById('ep-modal-backdrop').classList.remove('is-open');
      document.documentElement.classList.remove('is-clipped');
    }
    function closeEditProfileBackdrop(e) {
      if (e.target === document.getElementById('ep-modal-backdrop')) closeEditProfile();
    }

    function loadMoreScores(containerId, btnElement) {
      const container = document.getElementById(containerId);
      if (!container) return;
      const hiddenItems = container.querySelectorAll('.score-card[style*="display: none"]');
      let count = 0;
      for (let i = 0; i < hiddenItems.length; i++) {
        if (count < 10) {
          hiddenItems[i].style.display = '';
          count++;
        } else {
          break;
        }
      }
      const remaining = container.querySelectorAll('.score-card[style*="display: none"]').length;
      if (remaining === 0) {
        btnElement.style.display = 'none';
      }
    }

    function toggleBioEdit() {
      const display = document.getElementById('bio-display');
      const form = document.getElementById('bio-edit-form');
      if (display.style.display === 'none') {
        display.style.display = 'block';
        form.style.display = 'none';
      } else {
        display.style.display = 'none';
        form.style.display = 'block';
      }
    }

    function switchTab(tab) {
      document.getElementById('tab-sdvx').classList.remove('is-active');
      document.getElementById('tab-iidx').classList.remove('is-active');
      document.getElementById('pane-sdvx').style.display = 'none';
      document.getElementById('pane-iidx').style.display = 'none';
      
      document.getElementById('tab-' + tab).classList.add('is-active');
      document.getElementById('pane-' + tab).style.display = '';
    }

    document.querySelectorAll('.jb-modal').forEach(function(el) {
      el.addEventListener('click', function(e) {
        var target = document.getElementById(e.currentTarget.getAttribute('data-target'));
        if (target) {
          target.classList.add('is-active');
          document.documentElement.classList.add('is-clipped');
        }
      });
    });
    document.querySelectorAll('.jb-modal-close').forEach(function(el) {
      el.addEventListener('click', function(e) {
        e.currentTarget.closest('.modal').classList.remove('is-active');
        document.documentElement.classList.remove('is-clipped');
      });
    });
    // ---- Markdown toolbar helpers ----
    function getBioTA() { return document.getElementById('bio-textarea'); }

    function bioInsert(before, after) {
      var ta = getBioTA(); if (!ta) return;
      var start = ta.selectionStart, end = ta.selectionEnd;
      var sel = ta.value.slice(start, end);
      var replacement = before + (sel || 'text') + after;
      ta.setRangeText(replacement, start, end, 'select');
      // Reposition cursor inside the wrapper
      ta.selectionStart = start + before.length;
      ta.selectionEnd = start + before.length + (sel || 'text').length;
      ta.focus();
      bioAutoResize(ta);
    }

    function bioInsertLine(prefix) {
      var ta = getBioTA(); if (!ta) return;
      var start = ta.selectionStart;
      // Find start of current line
      var lineStart = ta.value.lastIndexOf('\n', start - 1) + 1;
      ta.setRangeText(prefix, lineStart, lineStart, 'end');
      ta.focus();
      bioAutoResize(ta);
    }

    function bioInsertLink() {
      var ta = getBioTA(); if (!ta) return;
      var start = ta.selectionStart, end = ta.selectionEnd;
      var sel = ta.value.slice(start, end) || 'link text';
      var url = prompt('Enter URL:', 'https://');
      if (!url) return;
      var md = '[' + sel + '](' + url + ')';
      ta.setRangeText(md, start, end, 'end');
      ta.focus();
      bioAutoResize(ta);
    }

    function bioAutoResize(ta) {
      ta.style.height = 'auto';
      ta.style.height = Math.min(ta.scrollHeight, 400) + 'px';
    }

    // Auto-resize on load and input
    document.addEventListener('DOMContentLoaded', function() {
      var ta = getBioTA();
      if (ta) {
        ta.addEventListener('input', function() { bioAutoResize(ta); });
        bioAutoResize(ta);
      }
    });

    function openSdvxScoreModal(el) {
      try {
        var data = JSON.parse(el.getAttribute('data-play'));
        if (!data) return;

        document.getElementById('modal-songname').innerText = data.title || 'Song Name';
        document.getElementById('modal-diff').innerText = (data.diff || 'MXM').toUpperCase();
        
        var jacketEl = document.getElementById('modal-jacket');
        if (jacketEl) jacketEl.src = data.jacketUrl || '';
        var avatarEl = document.getElementById('modal-player-avatar');
        if (avatarEl) avatarEl.src = MODAL_PLAYER_AVATAR;
        var nameEl = document.getElementById('modal-player-name');
        if (nameEl) nameEl.innerText = MODAL_PLAYER_NAME;
        
        var gradeStr = getSdvxGradeText(data.score, data.grade);
        var rankEl = document.getElementById('modal-rank');
        rankEl.innerText = gradeStr;
        rankEl.setAttribute('data-grade', gradeStr);

        document.getElementById('modal-score').innerText = Number(data.score || 0).toLocaleString();
        document.getElementById('modal-exscore').innerText = Number(data.exscore || 0).toLocaleString();
        document.getElementById('modal-maxchain').innerText = Number(data.maxChain || 0).toLocaleString();
        document.getElementById('modal-crit').innerText = Number(data.critical || 0).toLocaleString();
        document.getElementById('modal-near').innerText = Number(data.near || 0).toLocaleString();
        document.getElementById('modal-error').innerText = Number(data.error || 0).toLocaleString();
        document.getElementById('modal-early').innerText = Number(data.early || 0).toLocaleString();
        document.getElementById('modal-late').innerText = Number(data.late || 0).toLocaleString();

        var medalText = getSdvxMedalText(data.clear);
        document.getElementById('modal-medal').innerText = medalText;

        document.getElementById('score-detail-modal').classList.add('is-active');
        document.documentElement.classList.add('is-clipped');
      } catch(e) {
        console.error(e);
      }
    }

    function closeScoreModal() {
      var modal = document.getElementById('score-detail-modal');
      if (modal) modal.classList.remove('is-active');
      document.documentElement.classList.remove('is-clipped');
    }

    function getSdvxGradeText(score, rawGrade) {
      if (score >= 9900000) return 'S';
      if (score >= 9800000) return 'AAA+';
      if (score >= 9700000) return 'AAA';
      if (score >= 9500000) return 'AA+';
      if (score >= 9300000) return 'AA';
      if (score >= 9000000) return 'A+';
      if (score >= 8700000) return 'A';
      if (score >= 7500000) return 'B';
      if (score >= 6500000) return 'C';
      return 'D';
    }

    function getSdvxMedalText(clear) {
      var labels = ["NO DATA", "PLAYED", "EFFECTIVE CLEAR", "EXCESSIVE CLEAR", "MAXXIVE CLEAR", "ULTIMATE CHAIN", "PERFECT ULTIMATE CHAIN"];
      return labels[clear] || "PLAYED";
    }

    function getSdvxClearClassJS(clearVal) {
      var c = Number(clearVal);
      if (c === 5) return 'clear-puc';
      if (c === 4) return 'clear-uc';
      if (c === 3) return 'clear-exc';
      if (c === 2) return 'clear-comp';
      if (c === 1) return 'clear-fail';
      return '';
    }

    // Live update: when this profile's own refid just submitted a new SDVX
    // score (see _layout.pug's 'live-score' event, sourced from
    // /live/scores), prepend it to the Recent Plays list immediately instead
    // of waiting for a page refresh.
    window.addEventListener('live-score', function(e) {
      var evt = e.detail;
      if (!evt || evt.game !== 'sdvx' || String(evt.refid) !== String(PROFILE_REFID)) return;

      var list = document.getElementById('sdvx-recent-plays-list');
      if (!list) return;

      var play = {
        title: evt.title || ('Chart #' + evt.mid),
        diff: evt.diff || '',
        score: evt.score || 0,
        exscore: evt.exscore || 0,
        clear: evt.clear || 0,
        grade: evt.grade || 0,
        dateStr: 'Just now',
        jacketUrl: evt.jacketUrl || JACKET_DUMMY,
        isFirstPlace: false,
        maxChain: evt.maxChain || 0,
        critical: evt.critical || 0,
        near: evt.near || 0,
        error: evt.error || 0,
        early: evt.early || 0,
        late: evt.late || 0
      };

      var li = document.createElement('li');
      li.className = 'mb-3 live-recent-play' + (play.clear ? ' ' + getSdvxClearClassJS(play.clear) : '');
      li.style.cssText = 'padding-left: 10px; border-radius: 4px; cursor: pointer;';
      li.setAttribute('onclick', 'openSdvxScoreModal(this)');
      li.setAttribute('data-play', JSON.stringify(play));
      li.innerHTML =
        '<div class="is-flex is-align-items-center">' +
          '<img src="' + play.jacketUrl + '" onerror="jacketFallback(this)" style="width: 40px; height: 40px; border-radius: 4px; margin-right: 12px; object-fit: cover;">' +
          '<div style="flex: 1;">' +
            '<div>' +
              '<span class="icon is-small" style="color: #00d1b2;"><i class="mdi mdi-history"></i></span>' +
              '<span class="has-text-weight-bold">' + MODAL_PLAYER_NAME + '</span>' +
              ' played ' +
              '<span class="has-text-weight-bold" style="color: #66c2ff;">' + play.title + ' [' + play.diff + ']</span>' +
              ' - ' + play.score +
            '</div>' +
            '<div style="color: #888; font-size: 0.8rem;">Just now</div>' +
          '</div>' +
        '</div>';

      list.insertBefore(li, list.firstChild);
      while (list.children.length > 100) {
        list.removeChild(list.lastChild);
      }
    });

    function getIidxClearClassJS(clearVal) {
      var c = Number(clearVal);
      if (c === 7) return 'clear-fc';
      if (c === 6) return 'clear-exh';
      if (c === 5) return 'clear-hard';
      if (c === 4) return 'clear-clear';
      if (c === 3) return 'clear-easy';
      if (c === 2) return 'clear-assist';
      if (c === 1) return 'clear-fail';
      return '';
    }

    // Live update: same as the SDVX listener above, but for IIDX (no jacket
    // image/click modal for this section, matching its existing markup).
    window.addEventListener('live-score', function(e) {
      var evt = e.detail;
      if (!evt || evt.game !== 'iidx' || String(evt.refid) !== String(PROFILE_REFID)) return;

      var list = document.getElementById('iidx-recent-plays-list');
      if (!list) return;

      var play = {
        title: evt.title || ('Song #' + evt.mid),
        diff: evt.diff || '',
        score: evt.score || 0,
        clear: evt.clear || 0
      };

      var li = document.createElement('li');
      li.className = 'mb-2 live-recent-play' + (play.clear ? ' ' + getIidxClearClassJS(play.clear) : '');
      li.style.cssText = 'padding-left: 10px; border-radius: 4px;';
      li.innerHTML =
        '<span class="icon is-small" style="color: #00d1b2;"><i class="mdi mdi-history"></i></span>' +
        '<span class="has-text-weight-bold">' + MODAL_PLAYER_NAME + '</span>' +
        ' played ' +
        '<span class="has-text-weight-bold" style="color: #66c2ff;">' + play.title + ' [' + play.diff + ']</span>' +
        ' - ' + play.score +
        '<span class="is-pulled-right" style="color: #888; font-size: 0.8rem;">Just now</span>';

      list.insertBefore(li, list.firstChild);
      while (list.children.length > 100) {
        list.removeChild(list.lastChild);
      }
    });

block modal
  div.modal#score-detail-modal
    div.modal-background(onclick="closeScoreModal()")
    div.modal-content.score-modal-content
      div.score-header
        img.score-header-jacket#modal-jacket(src="" onerror="jacketFallback(this)" alt="")
        div.score-header-text
          h2#modal-songname Song Name
          div.score-subtitle
            span.diff-badge#modal-diff MXM
          div.score-played-by
            img.score-played-by-avatar#modal-player-avatar(src="" alt="")
            span.score-played-by-text played by 
            span.score-played-by-name#modal-player-name Player
      
      div.score-body
        div.score-rank-container
          div#modal-rank(data-grade="S") S

        div.score-stats
          div.score-number#modal-score 9,993,855
          div.score-meta-row
            div.score-meta-item
              span.score-meta-label EX Score
              span.score-meta-value#modal-exscore 0
            div.score-meta-item
              span.score-meta-label Max Combo
              span.score-meta-value#modal-maxchain 0
          div.score-medal-row
            span#modal-medal PLAYED

        div.judgements-container
          div.judgements-title Judgements
          div.judgement-row.j-row-crit
            span.j-dot
            span.j-label CRITICAL
            span.j-value#modal-crit 0
          div.judgement-row.j-row-near
            span.j-dot
            span.j-label NEAR
            span.j-value#modal-near 0
          div.j-sub-row#modal-near-breakdown
            span.j-sub-item
              span.arrow-early ▲
              span#modal-early 0
            span.j-sub-item
              span.arrow-late ▼
              span#modal-late 0

          div.judgement-row.j-row-error
            span.j-dot
            span.j-label ERROR
            span.j-value#modal-error 0

    button.modal-close.is-large(aria-label="close" onclick="closeScoreModal()")
